<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <div class="container">

    <% if (journeys.length === 0) { %>
      <div class="row">
        <div class="col-12 text-center">
          <p class="fs-2">Oops ! No trains available...</p>
        </div>
      </div>
      <div class="row">
        <div class="col-12 text-center">
          <a href="/home"><button class="btn btn-danger">Home</button></a>
        </div>
      </div>
    <% } else { %> 

    <div class="row">
      <div class="col-12">
        <p class="fs-2">Tickets available on <%= dateFormat %> :</p>
      </div>
    </div>

    <% for (var i = 0 ; i < journeys.length ; i++) { %> 
    <div class="row">
      <div class="col-12 d-flex justify-content-between align-items-center bg-white p-3 mb-3">
        <div class="fs-6 fw-bold"><%= journeys[i].departure %> / <%= journeys[i].arrival %> </div>
        <div class="text-secondary">train departure at : <%= journeys[i].departureTime %></div>
        <div class="fs-5 fw-bold"><%= journeys[i].price %> €</div>
        <button class="btn btn-danger">OK</button>
      </div>
    </div>
    <% } %> 
    <% } %> 

  </div>
  
</body>
</html>